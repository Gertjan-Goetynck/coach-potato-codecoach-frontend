import { TransformSessionStatusPipe } from './transform-session-status.pipe';

describe('TransformSessionStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformSessionStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
