import { FilterCoachByTopicPipe } from './filter-coach-by-topic.pipe';

describe('FilterCoachByTopicPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCoachByTopicPipe();
    expect(pipe).toBeTruthy();
  });
});
